// Stat/kit model shared with the Street Brawl engine (statValue, kitProfile), not a build generator on its own.
// The build-generation exports below are unused by Street Brawl but kept for the standalone generator scripts.
export { generateBuilds, generateBuild, statValue, choosePopulation, stylePopulations } from './build';
export { ARCHETYPES, WEIGHTS } from './stats';
export { kitProfile } from './kit';
